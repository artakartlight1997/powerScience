# v3 改訂スペック — 「文字を減らし、図で教える」

既存レッスンは文字が多すぎる。**説明を文章から図の中へ移す**改訂を行う。
内容の正確さは維持し、**分量だけを削る**。新しい知識を足す必要はない。

---

## 1. 数値基準（機械チェックあり・全項目必達）

| 項目 | 現状(平均) | 基準 |
|---|---|---|
| **読む文字**（地の文＋箇条書き＋表＋コールアウト） | 4,280字 | **2,200字以下** |
| 地の文 | 1,121字 | **800字以下** |
| 箇条書き | 729字 | 500字程度 |
| 表 | 626字 | 600字以下 |
| コールアウト（`> [!TIP]` など） | 1,802字 / 8個 | **600字以下 / 3個以下** |
| コード | 141行 | **70行以下** |
| figure ブロック | 12.4枚 | **10枚以上（減らさない）** |
| 1段落 | — | **100字以下** |
| 連続する段落 | — | **2つまで** |
| 全体の行数 | 308行 | **100〜210行** |
| H2 見出し | — | **5〜9個** |
| H1 / Mermaid | — | 禁止 |

**要点：読む文字を半分にする。図は減らさない。**

検査コマンド（提出前に必ず実行し、エラー0にすること）:

```bash
python3 scripts/check_density.py           # 全体
python3 scripts/check_density.py L0601     # 個別
```

---

## 2. 書き換えの型

各 H2 セクションは、原則この形にする。

```markdown
## 見出し（結論を書く）

1〜2行の導入。長い説明は書かない。

```figure
{ ...図。説明の主役はここ... }
```

- 箇条書き 3〜6個
- 1行40文字以内、1行1メッセージ
- 「〜だから〜になる」の因果は図の中へ
```

### やること

- **段落 → 図か箇条書きへ**。3行以上の段落は、ほぼ全部が図にできる
- **比較の文章 → `compare` / `matrix` / `tablediff`**
- **手順の文章 → `steps`**
- **並列した概念の説明 → `cards`**
- **because/therefore の連鎖 → `flow` / `pipeline`**
- **数式・DAXの解説 → `formula`**（注釈は図の中に入る）
- **数値例 → `chart` / `tablediff`**
- **判断の分岐 → `tree`**
- 表は残してよい。ただし**説明文を表のセルに移す**

### やらないこと

- 内容を削って浅くしない。**同じことを図で言う**
- 図の `caption` に長文を書かない（40文字以内）
- コードは残す。ただし**コードの前後の解説文は最小限**にし、注釈は `formula` 図へ
- コールアウト（`[!TIP]` など）は各レッスン **3個まで**。本文2行以内

---

## 3. 残すもの・削るもの

| 要素 | 扱い |
|---|---|
| 見出し構成 | ほぼ維持（多すぎる場合は統合） |
| 図 | **増やす** |
| 表 | 維持（説明文をセルに寄せる） |
| コード（DAX / M） | 維持。前後の説明文を削る |
| コールアウト | **3個まで**に絞る |
| 「理解度チェック」 | **3問→3問のまま維持**。ただし各1行 |
| 「よくあるエラー」表 | 維持 |
| 長い導入文 | **1〜2行に圧縮** |
| 同じ話の繰り返し | **削除** |
| 「〜と言えるでしょう」等の冗長表現 | **削除** |

---

## 4. 文体

- 短く言い切る。1文40文字以内を目安
- 体言止めを使ってよい
- 「〜することができます」→「〜できる」
- 「なぜなら〜だからです」→ 図の矢印ラベルへ
- 敬体（です・ます）は維持してよいが、簡潔に

---

## 5. 図の書き方

`AUTHORING_SPEC.md` の §5 がそのまま有効。13種類の `type` と `tone` を使う。
**`interactive` はすでに配置済みのものを消さないこと。**

図を増やすときの発想:

| 元の文章 | 変換先 |
|---|---|
| 「AにはXという利点があるが、Bのほうが〜」 | `compare` 2パネル |
| 「まず〜し、次に〜し、最後に〜する」 | `steps` |
| 「3つの方法がある。1つ目は〜」 | `cards` cols:3 |
| 「データ量が多く鮮度も必要なら〜」 | `matrix` 2×2 |
| 「この列をこう変換すると、こうなる」 | `tablediff` |
| 「この関数の第1引数は〜、第2引数は〜」 | `formula` |
| 「売上は家電52・衣料18・食品30」 | `chart` |
| 「条件によってAかBかCを選ぶ」 | `tree` |
| 「上位層が〜、下位層が〜」 | `stack` |
| 「1週目に〜、2週目に〜」 | `timeline` |
